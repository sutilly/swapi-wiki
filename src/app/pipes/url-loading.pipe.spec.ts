import { UrlLoadingPipe } from './url-loading.pipe';

describe('UrlLoadingPipe', () => {
  it('create an instance', () => {
    const pipe = new UrlLoadingPipe();
    expect(pipe).toBeTruthy();
  });
});
